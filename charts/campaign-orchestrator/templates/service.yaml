apiVersion: v1
kind: Service
metadata:
  name: {{ include "campaign-orchestrator.fullname" . }}
  labels:
    {{- include "campaign-orchestrator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "campaign-orchestrator.selectorLabels" . | nindent 4 }}
