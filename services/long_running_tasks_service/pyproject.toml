[project]
name = "long-running-tasks-service"
version = "0.1.0"
description = "Mock long-running tasks service"
requires-python = ">=3.10"
dependencies = [
    "intersect-sdk>=0.9.0",
    "pydantic",
]
