[project]
name = "random-number-service"
version = "0.1.0"
description = "Random number generator service"
requires-python = ">=3.10"
dependencies = [
    "intersect-sdk[amqp]>=0.9.0",
    "pydantic",
]
