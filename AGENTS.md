## Style Guide

- Do not use mocks when writing tests unless explicitly instructed to do so. Only generate mocks for specific external services mentioned; otherwise, assume that the external service exists in the testing environment.
